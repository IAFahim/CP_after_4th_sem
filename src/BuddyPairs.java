public class BuddyPairs {
    public static String buddy(long start, long limit) {
        long a = 0, b = 0;
        for (long i = start; i < limit; i++) {

        }
        return "(" + a + "" + b + ")";
    }
}
